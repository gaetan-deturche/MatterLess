[package]
name = "matterless-paint"
version = "0.1.0"
edition = "2024"

[dependencies]
cosmic-text = "0.19"
matterless-layout = { path = "../matterless-layout" }
matterless-render = { path = "../matterless-render" }

[dev-dependencies]
# Snapshots: a laid-out conversation written out as an image, so what the
# renderer produces can be looked at rather than only asserted about.
png = "0.18"
