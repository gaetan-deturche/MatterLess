[package]
name = "matterless-ui"
version = "0.1.0"
edition = "2024"

[dependencies]
# Text is measured, never guessed -- the same rule the message list is built on.
matterless-layout = { path = "../matterless-layout" }
