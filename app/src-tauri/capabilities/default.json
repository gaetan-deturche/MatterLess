{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the MatterLess window is allowed to ask for.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
